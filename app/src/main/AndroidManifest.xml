<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.foodhub">

    <!-- Permiso para acceder a Internet, necesario para consumir APIs, imágenes en red, etc. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Foodhub"
        tools:targetApi="31">

        <!-- Actividad principal: Login -->
        <activity
            android:name=".LoginActivity"
            android:label="Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad para registro de usuarios -->
        <activity
            android:name=".RegisterActivity"
            android:label="Register"
            android:exported="true" />

        <!-- Actividad principal luego del login -->
        <activity
            android:name=".MainActivity"
            android:label="FoodHub"
            android:exported="true" />

        <!-- Actividad de configuración de cuenta -->
        <activity
            android:name=".AccountSettingsActivity"
            android:exported="false" />

    </application>

</manifest>
